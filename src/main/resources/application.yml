spring:
  datasource:
    driver-class-name: 
    url: jdbc:mysql://localhost:3306/db_system?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      show-sql: false
    open-in-view: false
springdoc: 
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true
